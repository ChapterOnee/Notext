import("dissimuloLibs/arrays.ag");

header = getHeader();

header.setBackgroundColor("secondary");
header.setHoverEffectDisabled(true);

theme = header.getTheme();
theme = theme.toString();

print(theme);
